package shapes;
//2.Inside shapes directory, create a Measurable interface with the following methods:
//
//double getPerimeter();
//double getArea();

public interface Measurable {
    double getPerimeter();
    double getArea();
}